import funkin.ui.story.Level;
import funkin.save.Save;

class Week3Level extends Level
{
	function new()
	{
		super('week3');
	}

  override function isUnlocked():Bool {
    return Save.instance.hasBeatenLevel('week2');
  }
}
