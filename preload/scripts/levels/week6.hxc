import funkin.ui.story.Level;
import funkin.save.Save;

class Week6Level extends Level
{
	function new()
	{
		super('week6');
	}

  override function isUnlocked():Bool {
    return Save.instance.hasBeatenLevel('week5');
  }
}
