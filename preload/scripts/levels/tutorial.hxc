import funkin.ui.story.Level;
import funkin.save.Save;

class Week5Level extends Level
{
	function new()
	{
		super('tutorial');
	}

  override function isUnlocked():Bool {
    return true; // Better be set to true, because... you wouldn't be able to play the game without Tutorial or mods.
  }
}
